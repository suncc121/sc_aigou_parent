spring:
  cloud:
    config:
      uri: http://192.168.134.1:1299 #配置服务器
      label: master #分支
      name: zuul_application #github上面名称
      profile: dev #环境
      #profile: ${spring.actives.profile} #获取运行的参数
